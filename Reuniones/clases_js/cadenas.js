class Cadenas {

    static mayus = (cadena) => {
        return cadena.toUpperCase();
    }
     static minus = (cadena) => {
         return cadena.toLowerCase();
     }
     static evaluar = (cadena) => {
         return cadena ? true : false;
     }


}